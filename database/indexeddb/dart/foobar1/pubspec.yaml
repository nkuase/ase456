name: indexeddb_web_fixed
environment:
  sdk: '>=3.2.0 <4.0.0'
dependencies:
  web: ^1.1.1
  idb_shim: ^2.6.7  # or latest version
dev_dependencies:
  build_runner: ^2.3.3  # or latest version
  build_web_compilers: ^4.2.0  # or latest version
  test: ^1.21.0  # or latest version
